spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: javier10231927
    url: jdbc:mysql://localhost:3306/roulette?serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
